name: Build VisionClaw Unsigned IPA

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    # Use macos-15 which comes with Xcode 16 pre-installed
    runs-on: macos-15
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Xcode
        # On macos-15, the default is usually Xcode 16, but we'll ensure it's selected
        run: sudo xcode-select -s /Applications/Xcode_16.app

      # 1. CREATE SECRETS.SWIFT
      - name: Create Secrets.swift
        run: |
          DIRECTORY="samples/CameraAccess/CameraAccess"
          mkdir -p "$DIRECTORY"
          
          cat <<EOF > "$DIRECTORY/Secrets.swift"
          import Foundation

          enum Secrets {
            static let geminiAPIKey = "${{ secrets.GEMINI_API_KEY }}"
            static let openClawHost = "http://localhost.local"
            static let openClawPort = 18789
            static let openClawHookToken = "none"
            static let openClawGatewayToken = "none"
          }
          EOF
          echo "Secrets.swift created."

      # 2. INSTALL DEPENDENCIES
      - name: Install Dependencies
        run: |
          xcodebuild -resolvePackageDependencies \
            -project "samples/CameraAccess/CameraAccess.xcodeproj" \
            -scheme "CameraAccess"

      # 3. BUILD APP (FORCING IPHONE SDK)
      - name: Build App
        run: |
          xcodebuild build \
            -project "samples/CameraAccess/CameraAccess.xcodeproj" \
            -scheme "CameraAccess" \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES \
            COMPILER_INDEX_STORE_ENABLE=NO

      # 4. PACKAGE INTO IPA
      - name: Package into IPA
        run: |
          # Robust search for the .app file
          APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData -name "CameraAccess.app" -type d | head -n 1)
          
          if [ -z "$APP_PATH" ]; then
            echo "Error: Could not find CameraAccess.app in DerivedData"
            exit 1
          fi
          
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          zip -r VisionClaw_CameraAccess.ipa Payload

      # 5. UPLOAD ARTIFACT
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: VisionClaw-IPA
          path: VisionClaw_CameraAccess.ipa
